[build-system]
requires = ["setuptools>=42", "wheel", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = [
    "tests/unit",
    "tests/integration"
]

[tool.setuptools_scm]
local_scheme = "no-local-version"
write_to = 'esbuild/_version.py'
