from setuptools import setup

setup(
    name="esbuild",
    version="1.5.1",
    description="Repository for building the GDC Elasticsearch indices.",
    license="Apache",
    packages=["esbuild"],
)
