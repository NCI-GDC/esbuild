from setuptools import setup

setup(
    name="esbuild",
    version="0.1.0",
    description="Repository for building the GDC Elasticsearch indices.",
    license="Apache",
    packages=["esbuild"],
)
